FROM node:6

WORKDIR /XircleFeedbackBundle

# ENTRYPOINT [ "npm", "install" ]
# ENTRYPOINT [ "npm", "run", "bower-install" ]
# ENTRYPOINT [ "npm", "start" ]

# RUN npm install && npm run bower-install && npm start
